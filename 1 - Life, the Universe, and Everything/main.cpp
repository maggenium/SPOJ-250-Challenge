#include <iostream>
#include <bits/stdc++.h>
using namespace std;

// ifstream f("data.in");
// ofstream g("data.out");

int main() {
    int a;

    while(cin >> a) {
        if (a == 42) {
            return 0;
        }
        cout << endl << a;
    }
    return 0;
}
